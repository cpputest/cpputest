language: cpp
compiler:
  - clang
  - gcc

script:
  - wget https://googlemock.googlecode.com/files/gmock-1.6.0.zip
  - unzip gmock-1.6.0.zip
  - cd gmock-1.6.0 && ./configure && make
  - export GMOCK_HOME=gmock-1.6.0
  - mkdir temp_for_build && cd temp_for_build && ../configure && make check_all
